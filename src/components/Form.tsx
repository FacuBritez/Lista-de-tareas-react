import React from "react";
import '../stylesheets/Form.sass'

function Form() {
    return (
        <form>
            <input type="text" />
            <button>+</button>
            <button>Ordenar</button>
            <button>trash</button>
        </form>
    );
}

export default Form;